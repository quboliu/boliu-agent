# Rules

This directory contains reusable agent and repository rules.

## Current Rules

- [`repository-conventions.md`](repository-conventions.md): storage, validation, security, and change-discipline conventions for this repository.

Add one bounded rule document per concern. Keep project-specific implementation facts inside the relevant project or skill rather than turning them into global rules.
